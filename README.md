# signing-keys
Public keys for verifying cosign signatures on container images and other signed artifacts. Verifiers reference the raw key URLs from this repository in cosign verify --key … to confirm provenance from the official Azure DevOps build pipelines.
